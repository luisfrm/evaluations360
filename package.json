{
	"name": "nolatech",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"install-all": "cd client && npm install && cd ../backend && npm install",
		"install-backend": "cd backend && npm install",
		"install-frontend": "cd client && npm install",
		"start-all": "concurrently 'cd client && npm run dev' 'cd backend && npm run dev'",
		"start-frontend": "cd client && npm run dev",
		"start-backend": "cd backend && npm run dev",
		"build-backend": "cd backend && npm run build",
		"format": "prettier --write ."
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.1.2",
		"prettier": "^3.5.1"
	}
}
